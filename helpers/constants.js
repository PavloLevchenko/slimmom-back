const defaultPageSize = 20;
const defaultPage = 1;
const expiresAccessTime = '10m';
const expiresRefreshTime = '1d';

module.exports = {
  defaultPageSize,
  defaultPage,
  expiresAccessTime,
  expiresRefreshTime,
};
